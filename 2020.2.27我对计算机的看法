当我发现了bin里面存储了二进制文件，环境变量链接到某些二进制文件的文件夹，命令行的命令都是这些文件的文件名的时候，我突然对计算机有了点新的想法。
GUI式操作的电脑，当然就可以看作对和软件命令行的包装，所以我就直接说命令行了。
首先，计算机是能够处理二进制文件的机器，无论是编译型语言还是解释型语言，都会变成二进制文件被执行，而偏偏这些解释器，也都被写成了二进制文件放在了bin里面了
可以在命令行中调用它们
计算机仅能从命令行中接受命令
我认为，这样的封装，已经足够使计算机自洽且强大了，我甚至可以用编程语言自己造一些二进制文件来用
如此
我离完全控制一台计算机又近了一步。
